
module.exports = {
    RequestDrugModel: function(name, drugstore, count) {
      return requestDrugModel = {
        nameDrug: name,
        drugstore: drugstore,
        count: count  
      }
    }
  };
  
  
   